# 작업 폴더(원본 wide, 수백 MB) — git 제외. 동봉본은 data/raw/*.csv.gz.
nec_data/
nec_data_check/
*.parquet
# 비압축 가공본은 제외(압축본 data/processed/*.gz만 커밋)
data/processed/nec_2026_local.csv
data/nec_2026_local.csv

# T3 포렌식 플롯(out/*.png)은 공개 리포트 그림이라 커밋함.
# T6 이상치 CSV 등 그 외 생성물은 제외.
election_verify/out/*.csv
!election_verify/out/.gitkeep

# 실제 부족 투표소 목록 (직접 수집) — 예시만 커밋
data/shortage_list.csv

# 로그/임시 (단, 대시보드 index.html은 커밋)
*.log
/_*.html

# 파이썬
__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/

# OS
Thumbs.db
.DS_Store

# 스캔/전수조사 출력물(재생성 가능)
census_top2.txt
census_sig.txt
national_scan_out.txt
